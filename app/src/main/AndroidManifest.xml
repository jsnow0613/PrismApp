<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jsnow.prismsample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="com.jsnow.prismbase.base.BaseApp"
        android:theme="@style/AppTheme"
        tools:replace="android:icon"
        android:autoRevokePermissions="allowed">
        <activity android:name="com.jsnow.prismsample.guide.GuideActivity"></activity>
        <activity
            android:name="com.jsnow.prismsample.SplashActivity"
            android:label="PrismApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>